# 功能清单 · 按模块

> **维护者注意：** 本文档是项目功能的唯一权威清单。新增/变更功能时必须同步更新本清单（与 `shared/` 契约变更流程联动）。
> 功能编号规则：`模块前缀-序号`（如 F-AUTH-01），已发布的编号永不复用；需求废弃时标注「已废弃（日期）」并保留编号。

## 1. 账户与认证（AUTH）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-AUTH-01 | 双字段账号模型 | `loginName` 登录名是唯一凭据（用户不可自助修改）；`displayName` 姓名可自助修改；展示一律用姓名，统计/去重以登录名为准 | v1.0.0 |
| F-AUTH-02 | 登录与 JWT | POST /api/auth/login 签发 HS256 JWT（7 天，payload 含 sub/role/pwdVer） | v1.0.0 |
| F-AUTH-03 | 密码安全 | BCrypt 哈希校验；改密需旧密码、新密码 6~64 位；成功后 pwdVer+1，旧 token 全端立即失效并返回新 token | v1.0.0 |
| F-AUTH-04 | 独立会话 | ~~v1.0.0：web 与 admin 共享 cookie 单点登录~~ **v1.4.1 变更（D-016）**：web 与 admin 各自独立会话（token 存 `sessionStorage`，按浏览器标签页隔离），多标签登录不同账号互不顶线；401 统一跳各自登录页；Android 同账号体系本地存储 | v1.0.0 / 变更 v1.4.1 |
| F-AUTH-05 | 角色权限 | USER / ADMIN 两级；`/api/admin/**` 仅 ADMIN（role + 路径双重校验） | v1.0.0 |
| F-AUTH-06 | 种子账号 | 首次启动自动初始化 `admin/admin123`（ADMIN）+ 5 个演示账号（密码 123456） | v1.0.0 |
| F-AUTH-07 | 密码可逆存储 | passwordEnc（AES-256-GCM，密钥 `LUNCH_EXPORT_KEY`）仅供账户导出；登录仍走 BCrypt；存量账号无此字段时导出显示"—"。**安全权衡**：仅适合内网信任环境 | v1.2.0 |

## 2. 点餐核心（ORDER）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-ORDER-01 | 点餐窗口 | 默认 [14:00, 18:00] 闭区间（含两端），服务器 Asia/Shanghai 时间判定；窗口外不可登记/修改/取消（管理员不受限） | v1.0.0 |
| F-ORDER-02 | 登记/修改 | 每人每天最多一条记录；重复提交=修改（返回 updated 标志）；登记时选择要辣/不要辣 | v1.0.0 |
| F-ORDER-03 | 取消点餐 | 窗口内可取消；当天无记录返回 2002 | v1.0.0 |
| F-ORDER-04 | 全员实时看板数据 | `GET /api/orders/today/all`：当天名单/人数/要辣/不要辣统计，所有登录用户可查 | v1.1.0 |
| F-ORDER-05 | 文件存储 | 每天一个 `orders/YYYY-MM-DD.json`；原子写（临时文件+ATOMIC_MOVE）+ 进程内写锁；损坏自愈 | v1.0.0 |
| F-ORDER-07 | 管理员代取消 | `DELETE /api/admin/orders/today?loginName=`：管理员代取消指定用户当天订餐，**不受窗口限制**；中台今日看板名单行内操作 | v1.4.1 |
| F-ORDER-06 | 我的历史点餐 | `GET /api/me/orders?from=&to=`，缺省最近 30 天（跨度≤366 天），返回逐日记录 + 点餐/要辣/不要辣天数汇总 | v1.2.0 |

## 3. 聊天频道（CHAT）

> v1.3.0 需求变更：原「点餐评价」（星级+评语）重构为**公共聊天频道**；F-EVAL-\* 已废弃（编号保留不复用）。

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-EVAL-01 | 已废弃（v1.3.0）：当天星级评价 | 重构为 F-CHAT-01 | ~v1.2.0 |
| F-EVAL-02 | 已废弃（v1.3.0）：评价修改语义 | 由 F-CHAT-01 的追加式消息取代 | ~v1.2.0 |
| F-EVAL-03 | 已废弃（v1.3.0）：管理端按天查看评价 | 由 F-CHAT-02 取代 | ~v1.2.0 |
| F-CHAT-01 | 客户端当天聊天 | 所有登录用户可查看**当天**消息并发送（1~200 字，追加式存储）；仅当天可见，无历史入口 | v1.3.0 |
| F-CHAT-02 | 管理端历史记录 | 管理员可按天查看**任意历史日期**的聊天记录（`GET /api/admin/chat?date=`） | v1.3.0 |
| F-CHAT-03 | WebSocket 实时 IM | `WS /ws/chat?token=`（握手校验 JWT）：发送 `{"content"}` → 服务端持久化并**实时广播** `{"type":"chat","message":{...}}`；断线自动重连；进入页面 REST 拉取当天历史兜底；REST 发送保留兼容。消息按天存 `messages/YYYY-MM-DD.json` | WS v1.5.0 |
| F-CHAT-04 | Android 底部导航独立 Tab | 聊天升级为底部导航 4 Tab 之一（点餐 / 聊天 / 我的 / 设置）；微信式气泡布局（自己右侧蓝底、他人左侧灰底）；WS 断线自动重连 | v1.5.0 |

## 4. 统计与报表（REPORT）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-REPORT-01 | 今日看板（admin） | 统计牌（人数/要辣/不要辣）+ 辣度环形图 + 14:00-18:00 分 10 分钟桶的登记趋势折线 + 名单表（辣度筛选）；30 秒轮询保持实时 | v1.0.0 / 轮询 v1.1.0 |
| F-REPORT-02 | 历史统计（admin） | 日期范围查询：每日人数堆叠柱状图、点餐天数 TOP10、人员汇总表（含参与率进度条，分母含首尾两天） | v1.0.0 |
| F-REPORT-03 | 点餐统计导出 | `GET /api/admin/export`：xlsx 双 Sheet（点餐明细 / 人员汇总），中文文件名（RFC 5987） | v1.0.0 |
| F-REPORT-04 | 员工端实时面板 | 今日点餐情况卡片（统计标签 + 全员名单），30 秒轮询 + 本端操作后即时刷新 | v1.1.0 |

## 5. 用户管理（USER · 仅 ADMIN）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-USER-01 | 用户列表与单个创建 | 列表（角色标签 + 格式化创建时间）；单个创建（可选角色、密码 ≥6 位、登录名重复 1005） | v1.0.0 |
| F-USER-02 | 批量创建 | 文本多行 `登录名,姓名[,密码]`（中英文逗号/空白分隔，密码缺省 123456）；前端逐行预检 + 后端逐条创建；批内重复/与现有重复/非法行按条目失败，不影响成功条目 | v1.2.0 |
| F-USER-03 | 导出全部账户与密码 | `GET /api/admin/users/export`：xlsx（登录名/姓名/角色/密码/创建时间），密码由 passwordEnc 解密 | v1.2.0 |

## 6. 系统设置（SETTINGS · 仅 ADMIN）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-SET-01 | 点餐窗口在线设置 | `GET/PUT /api/admin/settings/window`：查看/修改窗口起止（HH:mm，start≤end 校验），写入 config.json；环境变量 `LUNCH_WINDOW_START/END` 优先级更高 | v1.2.0 |
| F-SET-02 | 定时通知设置 | `GET/PUT /api/admin/settings/notify`：提醒时间 + 通知标题（≤30 字）+ 通知内容（≤60 字），写入 config.json；Android 通过 `GET /api/settings/notify` 同步后本地调度（用户本地开关仍自控） | v1.3.0 |

## 7. 重要通知（NOTICE）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-NOTICE-01 | 通知数据与接口 | `notices.json` 单文件存储（自增 id）；`GET /api/notices` 所有登录用户可查（最新在前）；`POST/DELETE /api/admin/notices` 管理员发布/删除 | v1.4.0 |
| F-NOTICE-02 | 客户端顶部突出展示 | 登录后顶部横幅展示通知；**折叠时只显示最新一条**，可点击展开全部；无通知时不显示 | v1.4.0 |
| F-NOTICE-03 | 管理端管理 | 中台系统设置页：发布（标题≤50 字、内容≤500 字）、列表、删除 | v1.4.0 |

## 8. 员工 Web 端（WEB）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-WEB-01 | 登录/退出 | 登录页居中卡片；401 统一清 cookie 跳 `/login?redirect=`；顶部栏显示姓名 + 退出 | v1.0.0 |
| F-WEB-02 | 今日点餐页 | 窗口状态横幅（未开始/进行中/已截止）、距截止倒计时（serverTime 校准）、要点餐开关、要辣/不要辣、保存/取消（确认框）；窗口外表单禁用 | v1.0.0 |
| F-WEB-03 | 今日点餐情况面板 | 全员名单 + 统计标签，30 秒轮询 | v1.1.0 |
| F-WEB-04 | 聊天频道卡片 | 当天消息列表（姓名/时间/内容）+ 输入发送（≤200 字），15 秒轮询；次要位置（位于点餐与全员看板之后） | 聊天 v1.3.0 |
| F-WEB-05 | 我的 | 改姓名、改密码（成功后更新本地 token）、我的历史点餐（日期范围 + 汇总 + 明细） | 历史 v1.2.0 |
| F-WEB-06 | 重要通知横幅 | 顶部突出展示（登录后全局可见），折叠显示最新一条、可展开全部（F-NOTICE-02） | v1.4.0 |

## 9. Android 客户端（APP）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-APP-01 | 服务器地址配置 | 登录页「服务器设置」折叠区即可改 baseURL + 测试连接（任意 HTTP 响应即可达）；设置页内亦可改；自动补 http://、校验合法性 | v1.2.0 |
| F-APP-02 | 今日点餐 | 窗口状态横幅、倒计时（serverTime 校准）、登记/修改/取消 + 全员当日统计摘要（已订/要辣/不要辣 + 名单行） | 摘要 v1.1.0 |
| F-APP-03 | 已废弃（v1.3.0）：当天评价 | 重构为 F-APP-07 聊天频道入口 | ~v1.2.0 |
| F-APP-04 | 我的 | 改姓名、改密码（保存新 token）、历史点餐弹窗（汇总 + 逐日明细）、退出登录 | 历史 v1.2.0 |
| F-APP-05 | 每日提醒 | 提醒时间/标题/内容由管理员在中台配置（F-SET-02），客户端启动与登录时同步并按本地开关调度；默认每天 17:30；AlarmManager 精确闹钟（12+ 无权限降级并引导授权）；开机重注册；点击直达点餐页 | v1.0.0 / 同步 v1.3.0 |
| F-APP-06 | 设置页 | 服务器地址（保存 + 测试连接）、提醒开关与提醒时间（本地覆盖，服务端同步会更新时间） | v1.0.0 |
| F-APP-07 | 聊天频道（次要入口） | 今日点餐页保留次要入口按钮，聊天在独立页面（当天消息列表 + 发送 + 10 秒轮询）；UI 优先级：点餐功能 > 聊天 | v1.3.0 |
| F-APP-08 | 重要通知横幅 | 主界面顶部突出展示，折叠显示最新一条、可展开全部（F-NOTICE-02），拉取失败静默降级 | v1.4.0 |

## 10. 后台服务（SERVER）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-SRV-01 | 启动打印访问地址 | ApplicationReadyEvent：localhost + 局域网 IPv4 + 员工端/中台路径 + 数据目录 | v1.2.0 |
| F-SRV-02 | 数据目录与自愈 | `LUNCH_DATA_DIR`（默认 ./data）：users.json / config.json / orders/ / messages/ / notices.json；启动校验，单文件损坏告警并按空处理（users.json 损坏走重新种子），不静默清空其他数据 | v1.0.0 / messages v1.3.0 |
| F-SRV-03 | 统一错误码 | `{code, message}`；1001/1002/1003/1004/1005/2001/2002/3001/404/5000（见 shared/contracts/error-codes.md） | v1.0.0 |
| F-SRV-04 | SPA 路由回退与静态托管 | web 托管 `/`、admin 托管 `/admin/`；非 /api 无点 GET 回退到各自 index.html；单 jar 部署 | v1.0.0 |
| F-SRV-05 | CORS 与 Clock | /api 跨域全放开（开发用）；业务时间统一注入 Clock（生产固定 Asia/Shanghai，测试可固定时钟） | v1.0.0 |

## 11. 非功能需求（NFR）

| 编号 | 功能 | 说明 | 版本 |
|---|---|---|---|
| F-NFR-01 | 无数据库 | 全量业务数据 = `$LUNCH_DATA_DIR` 目录，拷贝即备份；禁止 ORM | v1.0.0 |
| F-NFR-02 | 内网定位 | 仅内网使用，中文界面，前端不引外网 CDN，无遥测 | v1.0.0 |
| F-NFR-03 | 测试基线 | 171 个单元/集成测试（server 53 / web 32 / admin 43 / android 43）；`scripts/check-all.sh` 一键回归；新增功能必须带测试 | v1.2.0 |
| F-NFR-04 | 协作模式 | Monorepo + `shared/` 契约单一事实来源 + CHANGELOG 决策记录（D-xxx）；各模块 README 登记契约版本 | v1.0.0 |
| F-NFR-05 | 开源 | MIT 协议；生产部署须注入 `LUNCH_JWT_SECRET` / `LUNCH_EXPORT_KEY` 并修改演示密码 | v1.2.0 |
